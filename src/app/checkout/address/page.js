import Navbar from "@/app/(navbar)/Navbar";
import React from "react";
import CheckOutAddress from "./Address";

const page = () => {
  return (
    <div>
      <Navbar />
      <CheckOutAddress />
    </div>
  );
};

export default page;
